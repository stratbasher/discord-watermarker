version: '3.8'

services:
  watermarker-bot:
    image: ghcr.io/<your-username>/<repo-name>:latest
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./tmp:/app/tmp
    restart: unless-stopped
    user: "1001:1001"